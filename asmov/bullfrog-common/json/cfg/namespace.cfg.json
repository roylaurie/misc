{
    "package": {
        "name": "common",
        "namespaces": {
            "common": {
                "desc": "Runs a common bullfrog operation.",
                "operations": {
                    "version": {
                        "desc": "Displays bullfrog version",
                        "parameters": {
                            "format": {
                                "desc": "Specifies the format",
                                "type": "string",
                                "required": false,
                                "position": 0
                            },
                            "service": {
                                "desc": "Turns on or off the version service",
                                "type": "boolean",
                                "enum": [ "off", "on" ],
                                "required": false,
                                "default": "off"
                            }
                        }
                    }
                }
            },
            "common.sysinfo": {
                "desc": "Provides system information tools",
                "operations": {
                    "default": {
                        "desc": "Displays a general profile of the system",
                        "parameters": {
                        }
                    }
                }
            }
        },
        "aliases": {
            "help": "common help"
        }
    }
}


