{
    "namespace": "common",
    "paths": {
        "common": {
            "script": "common.module.bash",
            "operations": {
                "version": {
                    "desc": "Displays bullfrog version",
                    "parameters": {}
                },
                "help": {
                    "desc": "Displays help on a topic",
                    "parameters": {
                        "namespace": { "index": 0, "default": "", "desc": "The namespace of the bullfrog operation to run. E.g., local.user" },
                        "operation": { "index": 1, "default": "", "desc": "The operation to run against a namespace. Omission will run the default operation." }
                    }
                }
            },
            "desc": "Runs a bullfrog operation."
        },
        "common.stats": {
            "script": "common.module.bash",
            "operations": {
                "default": {
                    "desc": "General statistics",
                    "parameters": {}
                }
            }
        }
    }
}

